/* const nombres = [];
let cantidadMaxima = 5 ;

for (let indice = 1; indice <= cantidadMaxima; indice++){
    let nombre = prompt('Ingresa un nombre');
    nombres.push(nombre);
    console.log("cantidad parcial: ", nombres.length);
}
console.log("Cantidad de nombres ", nombres.toString());

const slice = nombres.slice(2,4); 
console.log(slice.toString()); */
